# Legacy streaming golden

This curated snapshot contains the retired hand-specialized top and the three
custom/modified stream headers. It intentionally omits build directories,
shared libraries, logs, stock hls4ml headers, and copied `ap_types` headers.
