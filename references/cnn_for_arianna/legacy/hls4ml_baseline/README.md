# Legacy hls4ml baseline

This curated snapshot omits hls4ml's stock `ap_types` and `nnet_utils` headers.
It retains the generated top, parameters, configuration, and trained weights
needed to audit the pre-specialization network contract.
