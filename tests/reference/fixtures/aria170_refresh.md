# Aria 1.7.0 refresh fixture

`aria170_refresh.zip` was generated with the public `convert` API from RAVEL
commit `5fefb44`, using `two_block_c3.keras`, KU5P ffvb676 -2-e, 5 ns, P2/D1,
and required verification with Samples=8. The generated native downstream
project passed all 76 built-in vectors. Only the host-specific compiled shared
library is omitted; the recorded source closure remains complete and clean.

This historical artifact exercises public project opening and refresh across
optimization-policy changes. It is not a hand-edited manifest simulation.
